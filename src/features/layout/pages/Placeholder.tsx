import { Card, Typography } from 'antd';
import PageContainer from '@/shared/components/Layout/PageContainer';

const Placeholder = () => (
  <PageContainer>
    <Card>
      <Typography.Text>Placeholder de módulo. Integra siguiendo la guía.</Typography.Text>
    </Card>
  </PageContainer>
);

export default Placeholder;
